import brml.*
load("dataset.dat")
load("joint.dat")

assignments = zeros(12, 1);
givens = [8 11]; % input givens
queryVar = 1 + 1; % input query variable, and add 1 to it because
for g = givens
    assignments(g + 1) = 1;
end

for i = 