import brml.*;
load("EMprinter.mat");

EPS = 0.01;
N = size(x, 2); % get number of samples
K = 10;

t = 0;
cpt = 
diff = 0.02;
while diff > EPS % if true, we have not converged yet
    t = t+1;
    for n = 1:N
        % TODO: compute q_t^n % E step
    end
    for i = 1:K
        
    end
    diff = norm(newTheta - oldTheta, 1); % L1 norm
end